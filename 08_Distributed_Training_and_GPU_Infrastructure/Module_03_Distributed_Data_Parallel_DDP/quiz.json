[
  {
    "id": 1,
    "question": "Unused Parameters in DDP \u2014 In a multi-modal model, an image encoder is skipped for text-only samples. When training with PyTorch DDP, the training run hangs indefinitely. Why?",
    "category": "Unused Parameters in DDP",
    "options": [
      {
        "id": "opt_correct",
        "text": "DDP expects every registered parameter to fire its backward autograd hook in each step.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Standard BatchNorm computes mean and variance locally per GPU. When batch size per GPU shrinks to 2 samples, batch statistics become noisy and diverge across ranks. - Fix: Replace BatchNorm with GroupNorm/LayerNorm, or convert to torch.nn.SyncBatch",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 03 Distributed Data Parallel DDP.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 03 Distributed Data Parallel DDP.",
        "is_correct": false
      }
    ],
    "explanation": "DDP expects every registered parameter to fire its backward autograd hook in each step. A bucket only issues AllReduce when its parameter counter reaches 0. If the image encoder was skipped in the forward pass, its parameters receive no gradient and their hooks never fire! The bucket waits forever, causing a cluster-wide distributed deadlock! Fix: ",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 03 Distributed Data Parallel DDP failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": Standard BatchNorm computes mean and variance locally per GPU. When batch size per GPU shrinks to 2 samples, batch statistics become noisy and diverge across ranks. - Fix: Replace BatchNorm with GroupNorm/LayerNorm, or convert to torch.nn.SyncBatch",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Model accuracy degrades when scaling from 8 GPUs to 128 GPUs. - Root Cause: Standard BatchNorm computes mean and variance locally per GPU. When batch size per GPU shrinks to 2 samples, batch statistics become noisy and",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 03 Distributed Data Parallel DDP exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 03 Distributed Data Parallel DDP: : Standard BatchNorm computes mean and variance locally per GPU. When batch size per GPU shrinks to 2 samples, batch statistics become noisy and diverge across ranks. - Fix: Replace BatchNorm with GroupNorm/LayerNorm, or convert to torch.nn.SyncBatch. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]