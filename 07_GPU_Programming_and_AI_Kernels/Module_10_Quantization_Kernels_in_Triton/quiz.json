[
  {
    "id": 1,
    "question": "Compute Bound vs Memory Bound Quantization Regimes \u2014 When running batch size = 1 (single-user LLM generation), INT4 quantization provides a $3.5\\times$ speedup. When running batch size = 256 (high-concurrency serving), INT4 quantization is often slower than FP16. Why?",
    "category": "Compute Bound vs Memory Bound Quantization Regimes",
    "options": [
      {
        "id": "opt_correct",
        "text": "- Batch Size = 1: The GEMM is deeply Memory-Bound (Matrix-Vector multiply, $I \\approx 1$).",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Performing bitwise operations without proper signed two's complement extension: int4val = (packed >> 4) & 0x0F; // Treats 0x0F as +15 instead of -1! - Fix: Sign-extend using bit arithmetic: int8val = (int8t)(nibble << 4) >> 4; // Preserves sign bit",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 10 Quantization Kernels in Triton.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 10 Quantization Kernels in Triton.",
        "is_correct": false
      }
    ],
    "explanation": "- Batch Size = 1: The GEMM is deeply Memory-Bound (Matrix-Vector multiply, $I \\approx 1$). Memory bandwidth is the sole bottleneck. INT4 reduces memory traffic by $4\\times$, directly speeding up execution by $\\approx 3.5\\times$. - Batch Size = 256: The GEMM becomes Compute-Bound (high arithmetic intensity, $I > 100$). Memory bandwidth is no longer ",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 10 Quantization Kernels in Triton failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": Performing bitwise operations without proper signed two's complement extension: int4val = (packed >> 4) & 0x0F; // Treats 0x0F as +15 instead of -1! - Fix: Sign-extend using bit arithmetic: int8val = (int8t)(nibble << 4) >> 4; // Preserves sign bit",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Negative weights unpack as large positive integers (e.g. -1 becomes 15). - Root Cause: Performing bitwise operations without proper signed two's complement extension: int4val = (packed >> 4) & 0x0F; // Treats 0x0F as +",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 10 Quantization Kernels in Triton exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 10 Quantization Kernels in Triton: : Performing bitwise operations without proper signed two's complement extension: int4val = (packed >> 4) & 0x0F; // Treats 0x0F as +15 instead of -1! - Fix: Sign-extend using bit arithmetic: int8val = (int8t)(nibble << 4) >> 4; // Preserves sign bit. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]