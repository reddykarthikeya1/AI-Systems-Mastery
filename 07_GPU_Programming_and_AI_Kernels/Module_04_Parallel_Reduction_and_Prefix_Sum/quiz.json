[
  {
    "id": 1,
    "question": "Block Reduction with Grid-Stride Loop \u2014 You have an array of 100 million elements. How do you design a scalable 2-pass parallel reduction kernel without running out of SM resources?",
    "category": "Block Reduction with Grid-Stride Loop",
    "options": [
      {
        "id": "opt_correct",
        "text": "1. Pass 1 (Grid-Stride Block Reduction): - Launch a fixed grid of blocks (e.g. 512 blocks of 256 threads). - Each thread performs a grid-str...",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Passing 0xFFFFFFFF mask when some threads in the warp are inactive due to boundary conditions. - Fix: Always pass activemask() when calling warp shuffles inside divergent branch sections: val += shfldownsync(activemask(), val, offset);",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 04 Parallel Reduction and Prefix Sum.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 04 Parallel Reduction and Prefix Sum.",
        "is_correct": false
      }
    ],
    "explanation": "1. Pass 1 (Grid-Stride Block Reduction): - Launch a fixed grid of blocks (e.g. 512 blocks of 256 threads). - Each thread performs a grid-stride loop, accumulating multiple elements into its register (val += data[idx]). - Intra-block reduction: Threads use sequential addressing in shared memory down to 32 threads, then use shfldownsync within warp 0",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 04 Parallel Reduction and Prefix Sum failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": Passing 0xFFFFFFFF mask when some threads in the warp are inactive due to boundary conditions. - Fix: Always pass activemask() when calling warp shuffles inside divergent branch sections: val += shfldownsync(activemask(), val, offset);",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Undefined behavior or deadlock on Volta, Hopper, or Blackwell GPUs. - Root Cause: Passing 0xFFFFFFFF mask when some threads in the warp are inactive due to boundary conditions. - Fix: Always pass activemask() when call",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 04 Parallel Reduction and Prefix Sum exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 04 Parallel Reduction and Prefix Sum: : Passing 0xFFFFFFFF mask when some threads in the warp are inactive due to boundary conditions. - Fix: Always pass activemask() when calling warp shuffles inside divergent branch sections: val += shfldownsync(activemask(), val, offset);. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]