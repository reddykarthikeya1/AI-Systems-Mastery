[
  {
    "id": 1,
    "question": "Uncoalesced Read vs Write Penalty \u2014 In a matrix transpose kernel, you can choose between coalescing the global memory read and leaving the global memory write uncoalesced, or vice versa. Which one causes greater performance degradation?",
    "category": "Uncoalesced Read vs Write Penalty",
    "options": [
      {
        "id": "opt_correct",
        "text": "Uncoalesced writes cause greater performance degradation than uncoalesced reads.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Storing data as struct Particle { float x, y, z, mass; } particles[N];. - When threads read particles[i].x, Thread 0 reads byte 0, Thread 1 reads byte 16, Thread 2 reads byte 32. - Stride is 16 bytes, causing uncoalesced memory reads! - Fix: Conver",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 03 CUDA Memory Hierarchy and Coalescing.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 03 CUDA Memory Hierarchy and Coalescing.",
        "is_correct": false
      }
    ],
    "explanation": "Uncoalesced writes cause greater performance degradation than uncoalesced reads. - Reads: If threads issue strided loads, the L1/L2 cache lines can still provide spatial locality across subsequent warps. - Writes: Global memory writes that are uncoalesced cause multiple partial-cache-line write requests. On architectures with write-back caches, thi",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 03 CUDA Memory Hierarchy and Coalescing failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": Storing data as struct Particle { float x, y, z, mass; } particles[N];. - When threads read particles[i].x, Thread 0 reads byte 0, Thread 1 reads byte 16, Thread 2 reads byte 32. - Stride is 16 bytes, causing uncoalesced memory reads! - Fix: Conver",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Custom object kernel runs at 20% of memory bandwidth. - Root Cause: Storing data as struct Particle { float x, y, z, mass; } particles[N];. - When threads read particles[i].x, Thread 0 reads byte 0, Thread 1 reads byte",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 03 CUDA Memory Hierarchy and Coalescing exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 03 CUDA Memory Hierarchy and Coalescing: : Storing data as struct Particle { float x, y, z, mass; } particles[N];. - When threads read particles[i].x, Thread 0 reads byte 0, Thread 1 reads byte 16, Thread 2 reads byte 32. - Stride is 16 bytes, causing uncoalesced memory reads! - Fix: Conver. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]