[
  {
    "id": 1,
    "question": "FP8 Dynamic Range vs Precision (E4M3 vs E5M2) \u2014 In FlashAttention-3 with FP8 Tensor Cores, which FP8 format is selected for $Q, K, V$ matrix multiplication and why?",
    "category": "FP8 Dynamic Range vs Precision (E4M3 vs E5M2)",
    "options": [
      {
        "id": "opt_correct",
        "text": "NVIDIA Hopper supports two 8-bit floating point formats: 1.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": The transaction count passed to mbarrier.arriveandexpecttx(bytes) does not match the exact number of bytes transferred by the TMA instruction. - The hardware barrier never flips phase, so compute threads wait forever. - Fix: Verify that expectedbyt",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 09 FlashAttention 3 and Hopper Blackwell Features.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 09 FlashAttention 3 and Hopper Blackwell Features.",
        "is_correct": false
      }
    ],
    "explanation": "NVIDIA Hopper supports two 8-bit floating point formats: 1. E4M3 (1 sign, 4 exponent, 3 mantissa bits, bias=7): - Maximum representable value $\\approx 448$. Higher precision (3 mantissa bits). - Ideal for activations and weights where dynamic range is bounded. 2. E5M2 (1 sign, 5 exponent, 2 mantissa bits, bias=15): - Maximum value $\\approx 57{,}344",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 09 FlashAttention 3 and Hopper Blackwell Features failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": The transaction count passed to mbarrier.arriveandexpecttx(bytes) does not match the exact number of bytes transferred by the TMA instruction. - The hardware barrier never flips phase, so compute threads wait forever. - Fix: Verify that expectedbyt",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Kernel hangs indefinitely (deadlock) on H100 with 0% SM activity. - Root Cause: The transaction count passed to mbarrier.arriveandexpecttx(bytes) does not match the exact number of bytes transferred by the TMA instruct",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 09 FlashAttention 3 and Hopper Blackwell Features exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 09 FlashAttention 3 and Hopper Blackwell Features: : The transaction count passed to mbarrier.arriveandexpecttx(bytes) does not match the exact number of bytes transferred by the TMA instruction. - The hardware barrier never flips phase, so compute threads wait forever. - Fix: Verify that expectedbyt. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]