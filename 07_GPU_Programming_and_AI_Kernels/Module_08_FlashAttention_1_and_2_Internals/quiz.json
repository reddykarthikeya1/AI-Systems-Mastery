[
  {
    "id": 1,
    "question": "SRAM Block Sizing Constraints \u2014 How do you choose the block sizes $B_r$ and $B_c$ for FlashAttention on an NVIDIA A100 (164 KB Shared Memory per SM)?",
    "category": "SRAM Block Sizing Constraints",
    "options": [
      {
        "id": "opt_correct",
        "text": "Each SM must store: - $Qi$ block: $Br \\times d \\times 2 \\text{ bytes}$ (FP16) - $Kj$ block: $Bc \\times d \\times 2 \\text{ bytes}$ - $Vj$ block: $Bc \\times d \\times 2 \\text{ bytes}$ - Intermediate outputs / accumulators.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Iterating over all $j$ blocks even when $c{\\text{start}} > r{\\text{end}}$ where attention is completely masked out. - Fix: Skip entire blocks where $j \\times Bc > (i + 1) \\times Br$. For strictly lower-triangular blocks, bypass the softmax causal m",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It assumes state is eagerly cached in thread-local storage rather than evaluated per request in 08 FlashAttention 1 and 2 Internals.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "It causes an unchecked runtime exception due to memory alignment constraints in 08 FlashAttention 1 and 2 Internals.",
        "is_correct": false
      }
    ],
    "explanation": "Each SM must store: - $Qi$ block: $Br \\times d \\times 2 \\text{ bytes}$ (FP16) - $Kj$ block: $Bc \\times d \\times 2 \\text{ bytes}$ - $Vj$ block: $Bc \\times d \\times 2 \\text{ bytes}$ - Intermediate outputs / accumulators. For hidden dimension $d = 128$: If $Br = 64, Bc = 64$: Memory = $(64 \\times 128 + 64 \\times 128 + 64 \\times 128) \\times 2 = 49{,}15",
    "lesson_ref": "03_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When diagnosing the 08 FlashAttention 1 and 2 Internals failure 'Production Traps & Silent Failure Modes', what is the primary root cause and verified invariant?",
    "category": "Production Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": ": Iterating over all $j$ blocks even when $c{\\text{start}} > r{\\text{end}}$ where attention is completely masked out. - Fix: Skip entire blocks where $j \\times Bc > (i + 1) \\times Br$. For strictly lower-triangular blocks, bypass the softmax causal m",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": ": Causal attention runs at the same speed as bidirectional attention, wasting 50% FLOPs. - Root Cause: Iterating over all $j$ blocks even when $c{\\text{start}} > r{\\text{end}}$ where attention is completely masked out. -",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Lock contention on shared resource boundaries in 08 FlashAttention 1 and 2 Internals exhausts thread pool capacity.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Network socket timeouts trigger premature retry cascades across client workers.",
        "is_correct": false
      }
    ],
    "explanation": "In 08 FlashAttention 1 and 2 Internals: : Iterating over all $j$ blocks even when $c{\\text{start}} > r{\\text{end}}$ where attention is completely masked out. - Fix: Skip entire blocks where $j \\times Bc > (i + 1) \\times Br$. For strictly lower-triangular blocks, bypass the softmax causal m. Mitigation requires verifying system invariants before committing mutations.",
    "lesson_ref": "04_TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]