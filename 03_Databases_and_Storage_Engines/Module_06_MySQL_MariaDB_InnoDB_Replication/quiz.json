[
  {
    "id": 1,
    "question": "How does InnoDB's Clustered Index architecture differ from secondary indexes?",
    "category": "How does InnoDB's Clustered Index architecture differ from secondary indexes?",
    "options": [
      {
        "id": "opt_correct",
        "text": "The table's primary key forms the clustered index where leaf nodes store the actual row data.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "It writes dirty pages to a contiguous disk buffer before writing to primary data files, preventing page corruption from torn writes during crashes.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "SBR transmits SQL statements (prone to non-deterministic divergence, e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A unique identifier assigned to every committed transaction across a cluster, allowing seamless replica failover without parsing binlog file offsets.",
        "is_correct": false
      }
    ],
    "explanation": "The table's primary key forms the clustered index where leaf nodes store the actual row data. Secondary indexes store the primary key value as their pointer.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "What is the purpose of the InnoDB Doublewrite Buffer?",
    "category": "What is the purpose of the InnoDB Doublewrite Buffer?",
    "options": [
      {
        "id": "opt_correct",
        "text": "It writes dirty pages to a contiguous disk buffer before writing to primary data files, preventing page corruption from torn writes during crashes.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The table's primary key forms the clustered index where leaf nodes store the actual row data.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "SBR transmits SQL statements (prone to non-deterministic divergence, e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A unique identifier assigned to every committed transaction across a cluster, allowing seamless replica failover without parsing binlog file offsets.",
        "is_correct": false
      }
    ],
    "explanation": "It writes dirty pages to a contiguous disk buffer before writing to primary data files, preventing page corruption from torn writes during crashes.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "Explain the difference between Statement-Based Replication (SBR) and Row-Based Replication (RBR).",
    "category": "Explain the difference between Statement-Based Replication (SBR) and Row-Based Replication (RBR).?",
    "options": [
      {
        "id": "opt_correct",
        "text": "SBR transmits SQL statements (prone to non-deterministic divergence, e.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The table's primary key forms the clustered index where leaf nodes store the actual row data.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It writes dirty pages to a contiguous disk buffer before writing to primary data files, preventing page corruption from torn writes during crashes.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A unique identifier assigned to every committed transaction across a cluster, allowing seamless replica failover without parsing binlog file offsets.",
        "is_correct": false
      }
    ],
    "explanation": "SBR transmits SQL statements (prone to non-deterministic divergence, e.g. NOW()); RBR transmits exact before/after row byte changes.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]