[
  {
    "id": 1,
    "question": "How does SQLite Write-Ahead Logging (WAL) differ from the default ROLLBACK journal mode?",
    "category": "How does SQLite Write-Ahead Logging (WAL) differ from the default ROLLBACK journal mode?",
    "options": [
      {
        "id": "opt_correct",
        "text": "In WAL mode, changes are appended to a separate -wal file, allowing concurrent readers to read from the main database while a writer appends frames.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "No. Readers never block writers, and writers never block readers....",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It guarantees durability across application crashes, and calls fsync during checkpoints, providing an optimal balance of safety and speed.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "An active long-running read transaction holds an open read snapshot, preventing the checkpoint from truncating older frames.",
        "is_correct": false
      }
    ],
    "explanation": "In WAL mode, changes are appended to a separate -wal file, allowing concurrent readers to read from the main database while a writer appends frames.",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "Can readers block writers in SQLite WAL mode?",
    "category": "Can readers block writers in SQLite WAL mode?",
    "options": [
      {
        "id": "opt_correct",
        "text": "No. Readers never block writers, and writers never block readers....",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "In WAL mode, changes are appended to a separate -wal file, allowing concurrent readers to read from the main database while a writer appends frames.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It guarantees durability across application crashes, and calls fsync during checkpoints, providing an optimal balance of safety and speed.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "An active long-running read transaction holds an open read snapshot, preventing the checkpoint from truncating older frames.",
        "is_correct": false
      }
    ],
    "explanation": "No. Readers never block writers, and writers never block readers.",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "What does PRAGMA synchronous = NORMAL guarantee in WAL mode?",
    "category": "What does PRAGMA synchronous = NORMAL guarantee in WAL mode?",
    "options": [
      {
        "id": "opt_correct",
        "text": "It guarantees durability across application crashes, and calls fsync during checkpoints, providing an optimal balance of safety and speed.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "In WAL mode, changes are appended to a separate -wal file, allowing concurrent readers to read from the main database while a writer appends frames.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "No. Readers never block writers, and writers never block readers....",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "An active long-running read transaction holds an open read snapshot, preventing the checkpoint from truncating older frames.",
        "is_correct": false
      }
    ],
    "explanation": "It guarantees durability across application crashes, and calls fsync during checkpoints, providing an optimal balance of safety and speed.",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]