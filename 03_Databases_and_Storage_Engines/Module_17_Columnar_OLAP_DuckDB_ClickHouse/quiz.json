[
  {
    "id": 1,
    "question": "Why are columnar storage engines 10x-100x faster for analytical aggregations than row stores?",
    "category": "Why are columnar storage engines 10x-100x faster for analytical aggregations than row stores?",
    "options": [
      {
        "id": "opt_correct",
        "text": "Columnar stores read only the specific columns referenced in queries and use CPU SIMD vectorization to process thousands of values per instruction.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Processing data in batches of columnar vectors (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It sorts data by primary key, writes immutable sorted column parts, and merges them asynchronously in the background.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Projection pushdown reads only requested columns; predicate pushdown uses row-group min/max statistics to skip entire chunks of data without decompression.",
        "is_correct": false
      }
    ],
    "explanation": "Columnar stores read only the specific columns referenced in queries and use CPU SIMD vectorization to process thousands of values per instruction.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "What is Vectorized Execution in DuckDB?",
    "category": "What is Vectorized Execution in DuckDB?",
    "options": [
      {
        "id": "opt_correct",
        "text": "Processing data in batches of columnar vectors (e.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Columnar stores read only the specific columns referenced in queries and use CPU SIMD vectorization to process thousands of values per instruction.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It sorts data by primary key, writes immutable sorted column parts, and merges them asynchronously in the background.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Projection pushdown reads only requested columns; predicate pushdown uses row-group min/max statistics to skip entire chunks of data without decompression.",
        "is_correct": false
      }
    ],
    "explanation": "Processing data in batches of columnar vectors (e.g. 2,048 values in CPU L1/L2 cache) rather than executing an iterator loop one tuple at a time.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "How does the ClickHouse MergeTree storage engine organize data on disk?",
    "category": "How does the ClickHouse MergeTree storage engine organize data on disk?",
    "options": [
      {
        "id": "opt_correct",
        "text": "It sorts data by primary key, writes immutable sorted column parts, and merges them asynchronously in the background.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Columnar stores read only the specific columns referenced in queries and use CPU SIMD vectorization to process thousands of values per instruction.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Processing data in batches of columnar vectors (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Projection pushdown reads only requested columns; predicate pushdown uses row-group min/max statistics to skip entire chunks of data without decompression.",
        "is_correct": false
      }
    ],
    "explanation": "It sorts data by primary key, writes immutable sorted column parts, and merges them asynchronously in the background.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]