[
  {
    "id": 1,
    "question": "What is the formal difference between RANK() and DENSERANK() when ties occur in window ordering?",
    "category": "What is the formal difference between RANK() and DENSERANK() when ties occur in window ordering?",
    "options": [
      {
        "id": "opt_correct",
        "text": "RANK() leaves gaps in the sequence following ties (e.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Because in three-valued logic, comparison with NULL yields UNKNOWN.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "In modern query planners, non-recursive CTEs can be inlined or materialized as temp tables, whereas subqueries are typically inlined.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The recursive member executes repeatedly, taking the previous iteration's result as input until it returns an empty result set.",
        "is_correct": false
      }
    ],
    "explanation": "RANK() leaves gaps in the sequence following ties (e.g. 1, 2, 2, 4), while DENSERANK() leaves no gaps (e.g. 1, 2, 2, 3).",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "Why does WHERE id NOT IN (SELECT foreignid FROM t) evaluate to 0 rows if any foreignid is NULL?",
    "category": "Why does WHERE id NOT IN (SELECT foreignid FROM t) evaluate to 0 rows if any foreignid is NULL?",
    "options": [
      {
        "id": "opt_correct",
        "text": "Because in three-valued logic, comparison with NULL yields UNKNOWN.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "RANK() leaves gaps in the sequence following ties (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "In modern query planners, non-recursive CTEs can be inlined or materialized as temp tables, whereas subqueries are typically inlined.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The recursive member executes repeatedly, taking the previous iteration's result as input until it returns an empty result set.",
        "is_correct": false
      }
    ],
    "explanation": "Because in three-valued logic, comparison with NULL yields UNKNOWN. Any condition WHERE UNKNOWN is treated as false.",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "How does a Common Table Expression (CTE) differ from a subquery in execution optimization?",
    "category": "How does a Common Table Expression (CTE) differ from a subquery in execution optimization?",
    "options": [
      {
        "id": "opt_correct",
        "text": "In modern query planners, non-recursive CTEs can be inlined or materialized as temp tables, whereas subqueries are typically inlined.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "RANK() leaves gaps in the sequence following ties (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Because in three-valued logic, comparison with NULL yields UNKNOWN.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The recursive member executes repeatedly, taking the previous iteration's result as input until it returns an empty result set.",
        "is_correct": false
      }
    ],
    "explanation": "In modern query planners, non-recursive CTEs can be inlined or materialized as temp tables, whereas subqueries are typically inlined.",
    "lesson_ref": "06_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]