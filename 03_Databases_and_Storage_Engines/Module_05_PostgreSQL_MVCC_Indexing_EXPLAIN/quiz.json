[
  {
    "id": 1,
    "question": "How does PostgreSQL implement Multi-Version Concurrency Control (MVCC) without in-place updates?",
    "category": "How does PostgreSQL implement Multi-Version Concurrency Control (MVCC) without in-place updates?",
    "options": [
      {
        "id": "opt_correct",
        "text": "Every UPDATE writes a completely new tuple version to the heap page, populating xmin (creation transaction) and xmax (deletion transaction).",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "It tracks which heap pages contain only tuples visible to all current transactions, allowing index-only scans to skip checking heap pages.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Standard VACUUM marks dead tuple space as reusable without shrinking file size; VACUUM FULL rewrites the table into a new file and reclaims disk space under an exclusive lock.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Plain EXPLAIN shows planner estimates; ANALYZE actually executes the query and reports real runtime, while BUFFERS shows exact shared buffer cache hits and disk reads.",
        "is_correct": false
      }
    ],
    "explanation": "Every UPDATE writes a completely new tuple version to the heap page, populating xmin (creation transaction) and xmax (deletion transaction).",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "What is the purpose of the PostgreSQL Visibility Map?",
    "category": "What is the purpose of the PostgreSQL Visibility Map?",
    "options": [
      {
        "id": "opt_correct",
        "text": "It tracks which heap pages contain only tuples visible to all current transactions, allowing index-only scans to skip checking heap pages.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Every UPDATE writes a completely new tuple version to the heap page, populating xmin (creation transaction) and xmax (deletion transaction).",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Standard VACUUM marks dead tuple space as reusable without shrinking file size; VACUUM FULL rewrites the table into a new file and reclaims disk space under an exclusive lock.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Plain EXPLAIN shows planner estimates; ANALYZE actually executes the query and reports real runtime, while BUFFERS shows exact shared buffer cache hits and disk reads.",
        "is_correct": false
      }
    ],
    "explanation": "It tracks which heap pages contain only tuples visible to all current transactions, allowing index-only scans to skip checking heap pages.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "What happens during a standard VACUUM vs VACUUM FULL?",
    "category": "What happens during a standard VACUUM vs VACUUM FULL?",
    "options": [
      {
        "id": "opt_correct",
        "text": "Standard VACUUM marks dead tuple space as reusable without shrinking file size; VACUUM FULL rewrites the table into a new file and reclaims disk space under an exclusive lock.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Every UPDATE writes a completely new tuple version to the heap page, populating xmin (creation transaction) and xmax (deletion transaction).",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It tracks which heap pages contain only tuples visible to all current transactions, allowing index-only scans to skip checking heap pages.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Plain EXPLAIN shows planner estimates; ANALYZE actually executes the query and reports real runtime, while BUFFERS shows exact shared buffer cache hits and disk reads.",
        "is_correct": false
      }
    ],
    "explanation": "Standard VACUUM marks dead tuple space as reusable without shrinking file size; VACUUM FULL rewrites the table into a new file and reclaims disk space under an exclusive lock.",
    "lesson_ref": "05_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]