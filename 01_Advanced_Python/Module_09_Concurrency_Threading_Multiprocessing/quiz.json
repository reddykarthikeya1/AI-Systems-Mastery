[
  {
    "id": 1,
    "question": "Give two real-world examples of an I/O-bound task and two examples of a CPU-bound task in backend engineering.",
    "category": "Workload Classification:",
    "options": [
      {
        "id": "opt_correct",
        "text": "- I/O-Bound: Fetching data from an HTTP REST API; writing large files to an SSD.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The GIL is a mutex that prevents multiple native OS threads from executing CPython bytecode at the same time, protecting Python's internal memory management (reference counts) from corruption.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Whenever a thread executes an I/O operation (like waiting on network socket responses or disk reads), it releases the GIL, allowing other threads to run concurrently while the first thread waits.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A race condition occurs when two or more threads access and mutate shared data concurrently, causing non-deterministic data loss.",
        "is_correct": false
      }
    ],
    "explanation": "- I/O-Bound: Fetching data from an HTTP REST API; writing large files to an SSD. - CPU-Bound: Image compression / resizing; cryptographic hashing / mathematical matrix operations.",
    "lesson_ref": "09_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "What is the Global Interpreter Lock (GIL) in CPython, and why does it prevent multiple threads from executing pure CPU calculations simultaneously?",
    "category": "The GIL:",
    "options": [
      {
        "id": "opt_correct",
        "text": "The GIL is a mutex that prevents multiple native OS threads from executing CPython bytecode at the same time, protecting Python's internal memory management (reference counts) from corruption.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- I/O-Bound: Fetching data from an HTTP REST API; writing large files to an SSD.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Whenever a thread executes an I/O operation (like waiting on network socket responses or disk reads), it releases the GIL, allowing other threads to run concurrently while the first thread waits.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A race condition occurs when two or more threads access and mutate shared data concurrently, causing non-deterministic data loss.",
        "is_correct": false
      }
    ],
    "explanation": "The GIL is a mutex that prevents multiple native OS threads from executing CPython bytecode at the same time, protecting Python's internal memory management (reference counts) from corruption.",
    "lesson_ref": "09_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "If the GIL exists, why does ThreadPoolExecutor still speed up downloading 50 web pages by 10x?",
    "category": "I/O Concurrency:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Whenever a thread executes an I/O operation (like waiting on network socket responses or disk reads), it releases the GIL, allowing other threads to run concurrently while the first thread waits.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- I/O-Bound: Fetching data from an HTTP REST API; writing large files to an SSD.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The GIL is a mutex that prevents multiple native OS threads from executing CPython bytecode at the same time, protecting Python's internal memory management (reference counts) from corruption.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "A race condition occurs when two or more threads access and mutate shared data concurrently, causing non-deterministic data loss.",
        "is_correct": false
      }
    ],
    "explanation": "Whenever a thread executes an I/O operation (like waiting on network socket responses or disk reads), it releases the GIL, allowing other threads to run concurrently while the first thread waits.",
    "lesson_ref": "09_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]