[
  {
    "id": 1,
    "question": "What is the technical difference between opening a file in text mode (\"r\") versus binary mode (\"rb\")?",
    "category": "Stream Modes:",
    "options": [
      {
        "id": "opt_correct",
        "text": "- Text mode (\"r\") decodes raw bytes into Python str characters using an encoding (like UTF-8) and translates OS newline characters (\\r\\n $\\rightarrow$ \\n).",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Windows historically uses localized legacy code pages (like cp1252 or charmap) as its default encoding rather than UTF-8.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Using the forward slash / operator: fullpath = Path(\"parent\") / \"child\" / \"file.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Standard JSON specification does not define a native Date/Time data type.",
        "is_correct": false
      }
    ],
    "explanation": "- Text mode (\"r\") decodes raw bytes into Python str characters using an encoding (like UTF-8) and translates OS newline characters (\\r\\n $\\rightarrow$ \\n). - Binary mode (\"rb\") reads raw, uninterpreted bytes without decoding or altering byte sequences.",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "Why does failing to specify encoding=\"utf-8\" on Windows frequently result in UnicodeDecodeError when emojis or European currency symbols (\u20ac) are present?",
    "category": "Encoding Traps:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Windows historically uses localized legacy code pages (like cp1252 or charmap) as its default encoding rather than UTF-8.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- Text mode (\"r\") decodes raw bytes into Python str characters using an encoding (like UTF-8) and translates OS newline characters (\\r\\n $\\rightarrow$ \\n).",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Using the forward slash / operator: fullpath = Path(\"parent\") / \"child\" / \"file.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Standard JSON specification does not define a native Date/Time data type.",
        "is_correct": false
      }
    ],
    "explanation": "Windows historically uses localized legacy code pages (like cp1252 or charmap) as its default encoding rather than UTF-8. UTF-8 multi-byte characters cannot be decoded by legacy single-byte tables, raising UnicodeDecodeError.",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "In pathlib.Path, how do you join directory paths using modern pythonic operator overloading?",
    "category": "Path Composition:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Using the forward slash / operator: fullpath = Path(\"parent\") / \"child\" / \"file.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- Text mode (\"r\") decodes raw bytes into Python str characters using an encoding (like UTF-8) and translates OS newline characters (\\r\\n $\\rightarrow$ \\n).",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Windows historically uses localized legacy code pages (like cp1252 or charmap) as its default encoding rather than UTF-8.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Standard JSON specification does not define a native Date/Time data type.",
        "is_correct": false
      }
    ],
    "explanation": "Using the forward slash / operator: fullpath = Path(\"parent\") / \"child\" / \"file.txt\".",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]