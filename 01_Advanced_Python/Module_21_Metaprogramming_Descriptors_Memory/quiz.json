[
  {
    "id": 1,
    "question": "What does the 3-argument type(name, bases, namespacedict) call do at runtime?",
    "category": "Class Factories:",
    "options": [
      {
        "id": "opt_correct",
        "text": "It dynamically constructs and returns a new Python class object at runtime.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "A metaclass is a \"class of a class\"\u2014a callable that defines how class objects themselves are instantiated and customized during compilation.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "initsubclass is called on the parent class whenever a new child class inherits from it, allowing validation and auto-registration with simple, clean syntax.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. get(self, instance, owner) 2. set(self, instance, value) 3. delete(self, instance) 4. setname(self, owner, name)...",
        "is_correct": false
      }
    ],
    "explanation": "It dynamically constructs and returns a new Python class object at runtime.",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "What is a Metaclass, and what is the relationship between type and object?",
    "category": "Metaclasses:",
    "options": [
      {
        "id": "opt_correct",
        "text": "A metaclass is a \"class of a class\"\u2014a callable that defines how class objects themselves are instantiated and customized during compilation.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "It dynamically constructs and returns a new Python class object at runtime.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "initsubclass is called on the parent class whenever a new child class inherits from it, allowing validation and auto-registration with simple, clean syntax.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. get(self, instance, owner) 2. set(self, instance, value) 3. delete(self, instance) 4. setname(self, owner, name)...",
        "is_correct": false
      }
    ],
    "explanation": "A metaclass is a \"class of a class\"\u2014a callable that defines how class objects themselves are instantiated and customized during compilation.",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "How does initsubclass allow customizing subclass creation without writing a full metaclass?",
    "category": "Subclass Hooks:",
    "options": [
      {
        "id": "opt_correct",
        "text": "initsubclass is called on the parent class whenever a new child class inherits from it, allowing validation and auto-registration with simple, clean syntax.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "It dynamically constructs and returns a new Python class object at runtime.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "A metaclass is a \"class of a class\"\u2014a callable that defines how class objects themselves are instantiated and customized during compilation.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. get(self, instance, owner) 2. set(self, instance, value) 3. delete(self, instance) 4. setname(self, owner, name)...",
        "is_correct": false
      }
    ],
    "explanation": "initsubclass is called on the parent class whenever a new child class inherits from it, allowing validation and auto-registration with simple, clean syntax.",
    "lesson_ref": "08_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]