[
  {
    "id": 1,
    "tier": "Recall",
    "question": "Generators vs Functions: What is the fundamental difference in execution state between a function using return and a function using yield?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "- return terminates the function, destroys its local stack frame, and returns a final value. - yield produces a value, pauses the function, and preserves its entire execution state (variables, instruction pointer) so it can resume on the ne",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "return pauses the function and preserves its local state so it can be resumed later, while yield permanently destroys the stack frame and produces one final value.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Both return and yield destroy the function's local stack frame immediately; the only difference is that yield additionally logs the returned value to stdout for debugging.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "yield can only be used inside functions that also contain a return statement; calling a yield-only function without any return raises a SyntaxError at import time.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: - return terminates the function, destroys its local stack frame, and returns a final value. - yield produces a value, pauses the function, and preserves its entire execution state (variables, instruction pointer) so it can resume on the next next() call.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "tier": "Recall",
    "question": "Decorator Metadata: Why should you always decorate your inner wrapper with @functools.wraps(func)?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "functools.wraps recompiles the wrapper's bytecode to match the original function's bytecode exactly, so the wrapper executes at the same speed as calling the original function directly.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "It is only needed when the decorated function accepts keyword arguments; decorators wrapping functions that take purely positional arguments work identically with or without functools.wraps.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "functools.wraps registers the wrapper with Python's garbage collector so the original undecorated function is not freed from memory while the wrapper is still in use.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "tier": "Recall",
    "question": "Memory Footprint: Why does (x2 for x in range(10000000)) consume ~100 bytes of RAM, while [x2 for x in range(10000000)] consumes ~400 MB?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory. The list comprehension evaluates and stores all 10,000,000 integers in RAM immediately in $O(N)$ memory.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "Both expressions use identical memory because CPython always stores iterable elements in the same underlying array structure; the syntax only changes how many items are returned per next() call.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The generator expression computes all 10,000,000 values immediately just like the list comprehension, but stores them compressed on disk instead of in RAM, only decompressing values as they are consumed.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "range(10000000) itself is lazy, so wrapping it in either () or [] produces the same ~100 bytes; the 400 MB figure only appears if list() is called explicitly on the comprehension afterward.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory. The list comprehension evaluates and stores all 10,000,000 integers in RAM immediately in $O(N)$ memory.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 4,
    "tier": "Recall",
    "question": "The Iterator Protocol: What two magic methods must an object implement to satisfy Python's Iterator Protocol?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "1. iter(self): Must return the iterator object itself. 2. next(self): Must return the next value, or raise StopIteration when no items remain.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "1. __getitem__(self, index): Must support integer indexing starting at 0. 2. __len__(self): Must return the total number of items so Python knows when to stop.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "1. __next__(self): Must return the iterator object itself. 2. __iter__(self): Must return the next value, or raise StopIteration when exhausted -- the two names are the reverse of their real roles.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "1. __enter__(self): Must initialize the iteration state. 2. __exit__(self): Must clean up resources once StopIteration is raised, the same two methods used by context managers.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: 1. iter(self): Must return the iterator object itself. 2. next(self): Must return the next value, or raise StopIteration when no items remain.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 5,
    "tier": "Apply",
    "question": "Given the implementation in `test_log_analyzer.py`, what is the exact evaluated result of:\n\n```python\nsummary[\"total_errors_and_warnings\"]\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "0",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "1",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "2",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "None",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `0` as verified in test suite `test_log_analyzer.py`.",
    "lesson_ref": "test_log_analyzer.py"
  },
  {
    "id": 6,
    "tier": "Apply",
    "question": "Given the implementation in `test_log_analyzer.py`, what is the exact evaluated result of:\n\n```python\nfiltered\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "[]",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "[\"ERROR: disk full\"]",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "None",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Raises TypeError: 'NoneType' object is not iterable",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `[]` as verified in test suite `test_log_analyzer.py`.",
    "lesson_ref": "test_log_analyzer.py"
  },
  {
    "id": 7,
    "tier": "Apply",
    "question": "Given the implementation in `test_log_analyzer.py`, what is the exact evaluated result of:\n\n```python\ncompute_squares(5)\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "[0, 1, 4, 9, 16]",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "[1, 4, 9, 16, 25]",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "[0, 1, 4, 9, 16, 25]",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "<generator object compute_squares at 0x7f8a2c1b3450>",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `[0, 1, 4, 9, 16]` as verified in test suite `test_log_analyzer.py`.",
    "lesson_ref": "test_log_analyzer.py"
  },
  {
    "id": 8,
    "tier": "Apply",
    "question": "Given the implementation in `test_log_analyzer.py`, what is the exact evaluated result of:\n\n```python\nlen(created_path.read_text(encoding=\"utf-8\").splitlines())\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "100",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "101",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "99",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "200",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `100` as verified in test suite `test_log_analyzer.py`.",
    "lesson_ref": "test_log_analyzer.py"
  },
  {
    "id": 9,
    "tier": "Diagnose",
    "question": "During triage in Decorators Generators Context Managers, an engineer observes:\n\"Introspection and docstring loss: parseaccesslog.name returns 'wrapper' instead of 'parseaccesslog'.\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "A custom decorator replaces the decorated function with wrapper. Without functools.wraps(func), metadata (name, doc, annotations) is replaced by the wrapper's metadata.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "parse_access_log.__name__ always reflects the name of the last decorator applied in the stack, regardless of functools.wraps, so 'wrapper' is expected whenever more than one decorator is stacked.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Python renames any function assigned to a variable called wrapper inside a decorator, permanently overwriting its __name__ attribute in the function's own module namespace.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "The decorator was applied using the @decorator syntax instead of parse_access_log = decorator(parse_access_log); the explicit assignment form is required to preserve the original __name__.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: A custom decorator replaces the decorated function with wrapper. Without functools.wraps(func), metadata (name, doc, annotations) is replaced by the wrapper's metadata.\n\nFix Resolution:\nDecorate the inner wrapper with @functools.wraps(func):",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 10,
    "tier": "Diagnose",
    "question": "During triage in Decorators Generators Context Managers, an engineer observes:\n\"Generator produces empty results on second pass: Processing the stream a second time returns\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "Generators produce values on demand and maintain internal state (giframe). Once exhausted (StopIteration), they cannot be re-iterated.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The stream was opened in text mode instead of binary mode, so re-reading it a second time returns an empty string instead of raising StopIteration.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "process_stream caches its first return value with functools.lru_cache, so calling it again with the same stream object returns the cached (now-empty) result instead of recomputing.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "The generator function itself contains a bug where its internal counter variable is a mutable default argument, so it silently resets to a stale empty state on every call after the first.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: Generators produce values on demand and maintain internal state (giframe). Once exhausted (StopIteration), they cannot be re-iterated.\n\nFix Resolution:\nEither convert to a list upfront if memory permits, or pass an iterable/factory that creates a fresh generator per pass:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 11,
    "tier": "Diagnose",
    "question": "During triage in Decorators Generators Context Managers, an engineer observes:\n\"Critical exceptions silently swallowed: A fatal KeyError inside a database block is swallowed, masking critical data corruption.\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "In Python's context management protocol, if exit returns a truthy value, Python suppresses the exception that occurred inside the with block.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The except clause inside the context manager's __exit__ catches the KeyError and re-raises it as a generic Exception, which the calling code's specific except KeyError: no longer matches.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The with block was written without an as clause, so the database connection object is never bound to a name and cleanup silently fails without propagating any error.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "logging.exception() was called inside __exit__ before returning None, and because the exception was already logged, Python treats it as handled and does not re-raise it.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: In Python's context management protocol, if exit returns a truthy value, Python suppresses the exception that occurred inside the with block.\n\nFix Resolution:\nOnly return True for specifically handled exception types; return False (or None) by default:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  }
]