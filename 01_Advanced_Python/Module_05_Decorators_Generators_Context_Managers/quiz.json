[
  {
    "id": 1,
    "question": "What is the fundamental difference in execution state between a function using return and a function using yield?",
    "category": "Generators vs Functions:",
    "options": [
      {
        "id": "opt_correct",
        "text": "- return terminates the function, destroys its local stack frame, and returns a final value.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. iter(self): Must return the iterator object itself. 2. next(self): Must return the next value, or raise StopIteration when no items remai...",
        "is_correct": false
      }
    ],
    "explanation": "- return terminates the function, destroys its local stack frame, and returns a final value. - yield produces a value, pauses the function, and preserves its entire execution state (variables, instruction pointer) so it can resume on the next next() call.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "Why should you always decorate your inner wrapper with @functools.wraps(func)?",
    "category": "Decorator Metadata:",
    "options": [
      {
        "id": "opt_correct",
        "text": "It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- return terminates the function, destroys its local stack frame, and returns a final value.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. iter(self): Must return the iterator object itself. 2. next(self): Must return the next value, or raise StopIteration when no items remai...",
        "is_correct": false
      }
    ],
    "explanation": "It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "Why does (x2 for x in range(10000000)) consume ~100 bytes of RAM, while [x2 for x in range(10000000)] consumes ~400 MB?",
    "category": "Memory Footprint:",
    "options": [
      {
        "id": "opt_correct",
        "text": "The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- return terminates the function, destroys its local stack frame, and returns a final value.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It copies the original function's metadata (name, doc, annotations, and module) to the wrapper, preventing debugging tools, IDE autocomplete, and test frameworks from seeing a generic 'wrapper' function.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "1. iter(self): Must return the iterator object itself. 2. next(self): Must return the next value, or raise StopIteration when no items remai...",
        "is_correct": false
      }
    ],
    "explanation": "The generator expression calculates values lazily on demand one item at a time in $O(1)$ memory. The list comprehension evaluates and stores all 10,000,000 integers in RAM immediately in $O(N)$ memory.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]