[
  {
    "id": 1,
    "question": "What is the technical difference in responsibility between new and init when creating a new class instance?",
    "category": "Object Lifecycle:",
    "options": [
      {
        "id": "opt_correct",
        "text": "- new(cls) is the constructor: it allocates the raw memory and returns the newly created instance.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Use @classmethod when you need access to the class object itself (cls) rather than a specific instance\u2014most commonly to implement alternative factory constructors (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It triggers Name Mangling: Python automatically prefixes the attribute name with ClassName (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Properties maintain a clean, pythonic public API (account.",
        "is_correct": false
      }
    ],
    "explanation": "- new(cls) is the constructor: it allocates the raw memory and returns the newly created instance. - init(self) is the initializer: it accepts the newly allocated instance and configures its attributes.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "When should you use a @classmethod instead of a standard instance method or @staticmethod?",
    "category": "Method Types:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Use @classmethod when you need access to the class object itself (cls) rather than a specific instance\u2014most commonly to implement alternative factory constructors (e.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- new(cls) is the constructor: it allocates the raw memory and returns the newly created instance.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "It triggers Name Mangling: Python automatically prefixes the attribute name with ClassName (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Properties maintain a clean, pythonic public API (account.",
        "is_correct": false
      }
    ],
    "explanation": "Use @classmethod when you need access to the class object itself (cls) rather than a specific instance\u2014most commonly to implement alternative factory constructors (e.g., Date.fromtimestamp(ts) or User.fromjson(raw)).",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "In Python, what does the double leading underscore (e.g. self.secretkey) actually do to the variable name?",
    "category": "Encapsulation:",
    "options": [
      {
        "id": "opt_correct",
        "text": "It triggers Name Mangling: Python automatically prefixes the attribute name with ClassName (e.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- new(cls) is the constructor: it allocates the raw memory and returns the newly created instance.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Use @classmethod when you need access to the class object itself (cls) rather than a specific instance\u2014most commonly to implement alternative factory constructors (e.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "Properties maintain a clean, pythonic public API (account.",
        "is_correct": false
      }
    ],
    "explanation": "It triggers Name Mangling: Python automatically prefixes the attribute name with ClassName (e.g. self.secretkey becomes self.Accountsecretkey) to prevent accidental naming collisions in subclasses.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]