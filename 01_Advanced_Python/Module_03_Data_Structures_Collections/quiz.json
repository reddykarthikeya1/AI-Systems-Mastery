[
  {
    "id": 1,
    "tier": "Recall",
    "question": "Time Complexity: Why is accessing a list element by index (lst[500]) an $O(1)$ constant time operation, while finding an element by value (500 in lst) is $O(N)$ linear time?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "- A list is stored in contiguous RAM. Accessing by index uses pointer arithmetic: address = base + (index 8 bytes), which computes immediately in $O(1)$. - Finding by value requires scanning every element sequentially from index $0$ to $N-1",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "Both lst[500] and 500 in lst are O(1) because CPython caches the memory address of every element the first time the list is created, so later lookups by either index or value are instant hash-table lookups.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "lst[500] is O(N) because Python must walk from the head of the list to the 500th node, exactly like a linked list; 500 in lst is O(1) because membership tests use a hidden hash index built automatically for every list.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Both operations are O(log N) because CPython stores list elements in a sorted, balanced binary tree internally, allowing binary search for both direct indexing and value lookups.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: - A list is stored in contiguous RAM. Accessing by index uses pointer arithmetic: address = base + (index 8 bytes), which computes immediately in $O(1)$. - Finding by value requires scanning every element sequentially from index $0$ to $N-1$, taking $O(N)$ linear time.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "tier": "Recall",
    "question": "List Internals: Why does Python over-allocate memory when growing a list with append(), rather than allocating exact space for one item at a time?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "Python over-allocates because append() always doubles the list's memory in-place without moving any existing elements, and no reallocation ever happens after the very first over-allocation.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The over-allocated slots are required by CPython's garbage collector, which needs spare capacity in every list to run its mark-and-sweep cycle without pausing the interpreter.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Over-allocation exists purely so that list.insert(0, x) can shift elements right without needing a temporary buffer; append() itself always allocates exact space for one new item.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "tier": "Recall",
    "question": "Immutability & Hashing: Why does Python raise a TypeError if you try to use a list as a dictionary key, but allows tuples?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "Dictionary keys must be hashable (their hash value must never change over their lifetime). Lists are mutable (elements can be added/removed, changing their identity), whereas tuples are immutable.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "Lists raise TypeError because they don't define __eq__, while tuples define both __eq__ and __hash__; hashability has nothing to do with mutability in either case.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Both lists and tuples are hashable by their memory address; the TypeError instead comes from dict keys being required to be sortable, and lists don't support the < operator between elements of different types.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Dictionaries actually accept lists as keys as long as every element inside the list is itself hashable; the TypeError only appears when a list contains another unhashable list nested inside it.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: Dictionary keys must be hashable (their hash value must never change over their lifetime). Lists are mutable (elements can be added/removed, changing their identity), whereas tuples are immutable.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 4,
    "tier": "Recall",
    "question": "Queue Performance: If you need a FIFO (First-In, First-Out) queue that processes 1,000,000 items, why should you use collections.deque instead of a standard list?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "list.pop(0) takes $O(N)$ time because removing the first element requires shifting all remaining elements to the left. collections.deque is a doubly-linked list of memory blocks providing $O(1)$ instant pops from both ends.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "list.pop(0) is O(1) because CPython keeps a head pointer for every list, but collections.deque is preferred anyway because it also supports O(1) random access by index, which lists lack.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Both list.pop(0) and deque.popleft() are O(N); deque is only faster in practice because it stores items in a contiguous array like a list but pre-allocates extra head-room to avoid a full list copy on append.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "deque is preferred only because it enforces a maximum length automatically, preventing memory leaks; its actual pop/append performance is identical to a plain list's O(N) behavior at both ends.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: list.pop(0) takes $O(N)$ time because removing the first element requires shifting all remaining elements to the left. collections.deque is a doubly-linked list of memory blocks providing $O(1)$ instant pops from both ends.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 5,
    "tier": "Apply",
    "question": "Given the implementation in `test_matching_engine.py`, what is the exact evaluated result of:\n\n```python\ntrades[0].price\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "100.0",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "100.5",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "200.0",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "0.0",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `100.0` as verified in test suite `test_matching_engine.py`.",
    "lesson_ref": "test_matching_engine.py"
  },
  {
    "id": 6,
    "tier": "Apply",
    "question": "Given the implementation in `test_matching_engine.py`, what is the exact evaluated result of:\n\n```python\ndepth[\"bids\"][0]\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "(11.50, 5)",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "(11.50, 10)",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "(11.00, 5)",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "[11.50, 5]",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `(11.50, 5)` as verified in test suite `test_matching_engine.py`.",
    "lesson_ref": "test_matching_engine.py"
  },
  {
    "id": 7,
    "tier": "Apply",
    "question": "Given the implementation in `test_matching_engine.py`, what is the exact evaluated result of:\n\n```python\ndepth[\"asks\"]\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "[(100.0, 10)]",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "[]",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "None",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Raises IndexError: list index out of range",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `[(100.0, 10)]` as verified in test suite `test_matching_engine.py`.",
    "lesson_ref": "test_matching_engine.py"
  },
  {
    "id": 8,
    "tier": "Apply",
    "question": "Given the implementation in `test_matching_engine.py`, what is the exact evaluated result of:\n\n```python\ntrades[0].quantity\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "5",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "6",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "4",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "10",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `5` as verified in test suite `test_matching_engine.py`.",
    "lesson_ref": "test_matching_engine.py"
  },
  {
    "id": 9,
    "tier": "Diagnose",
    "question": "During triage in Data Structures Collections, an engineer observes:\n\"Mutating cloned portfolio corrupts original portfolio: Modifying cloned[\"trader\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "copy.copy() duplicates the top-level container, but the nested list and dict objects inside it remain pointers to the identical memory blocks.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "copy.copy() actually creates a fully independent deep clone of the portfolio; the corruption instead comes from both variables being reassigned to the same dict literal earlier in the script.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The clone was created with dict(portfolio) instead of copy.copy(), and dict() silently returns a reference to the original object instead of building a new top-level mapping.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "portfolio was declared as a class attribute rather than an instance attribute, so every clone created from the class automatically shares one dictionary regardless of how the copy was made.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: copy.copy() duplicates the top-level container, but the nested list and dict objects inside it remain pointers to the identical memory blocks.\n\nFix Resolution:\nUse copy.deepcopy() to create an isolated clone of nested objects:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 10,
    "tier": "Diagnose",
    "question": "During triage in Data Structures Collections, an engineer observes:\n\"TypeError inserting records into set:\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "Sets require elements to implement hash and be immutable. Python dicts are mutable and do not have a hash method.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "set() only accepts elements of a single consistent type; the TypeError is raised because the collection mixes dict and str records instead of because dicts are unhashable.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Sets silently convert unhashable elements like dict into a hashable proxy in older Python versions, but this trader-record schema disables that conversion by defining a custom __eq__ without __hash__.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "The TypeError comes from calling set.add() with two positional arguments instead of one; a set element must be passed as a single object, not spread across multiple arguments.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: Sets require elements to implement hash and be immutable. Python dicts are mutable and do not have a hash method.\n\nFix Resolution:\nStore an immutable hashable representation, such as a tuple of sorted items or frozen dataclass:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 11,
    "tier": "Diagnose",
    "question": "During triage in Data Structures Collections, an engineer observes:\n\"RuntimeError on dictionary eviction:\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "Python's dictionary iterators guard against concurrent modifications. Deleting a key while iterating over ordersdict.items() invalidates the internal hashtable iteration pointer.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "orders_dict.items() returns a fully materialized list snapshot in Python 3, so deleting a key mid-loop should be safe; the RuntimeError is instead caused by iterating the dict while another thread holds the GIL.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Deleting a key changes the dict's insertion order, and Python 3.7+ dicts raise RuntimeError whenever iteration order is altered, even if no keys are added or removed.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "The RuntimeError only occurs because the loop uses del orders_dict[k] instead of orders_dict.pop(k); pop() is documented as the sole safe way to remove a key during iteration.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: Python's dictionary iterators guard against concurrent modifications. Deleting a key while iterating over ordersdict.items() invalidates the internal hashtable iteration pointer.\n\nFix Resolution:\nIterate over a static snapshot of keys or use a dictionary comprehension:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  }
]