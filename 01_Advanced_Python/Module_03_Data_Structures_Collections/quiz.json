[
  {
    "id": 1,
    "question": "Why is accessing a list element by index (lst[500]) an $O(1)$ constant time operation, while finding an element by value (500 in lst) is $O(N)$ linear time?",
    "category": "Time Complexity:",
    "options": [
      {
        "id": "opt_correct",
        "text": "- A list is stored in contiguous RAM.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Dictionary keys must be hashable (their hash value must never change over their lifetime).",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "list.pop(0) takes $O(N)$ time because removing the first element requires shifting all remaining elements to the left. collections.deque is ...",
        "is_correct": false
      }
    ],
    "explanation": "- A list is stored in contiguous RAM. Accessing by index uses pointer arithmetic: address = base + (index 8 bytes), which computes immediately in $O(1)$. - Finding by value requires scanning every element sequentially from index $0$ to $N-1$, taking $O(N)$ linear time.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "question": "Why does Python over-allocate memory when growing a list with append(), rather than allocating exact space for one item at a time?",
    "category": "List Internals:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- A list is stored in contiguous RAM.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Dictionary keys must be hashable (their hash value must never change over their lifetime).",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "list.pop(0) takes $O(N)$ time because removing the first element requires shifting all remaining elements to the left. collections.deque is ...",
        "is_correct": false
      }
    ],
    "explanation": "Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "question": "Why does Python raise a TypeError if you try to use a list as a dictionary key, but allows tuples?",
    "category": "Immutability & Hashing:",
    "options": [
      {
        "id": "opt_correct",
        "text": "Dictionary keys must be hashable (their hash value must never change over their lifetime).",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "- A list is stored in contiguous RAM.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "Over-allocating memory ensures that most append() operations simply write to already-reserved memory without needing expensive operating system memory reallocations, giving append() an amortized $O(1)$ running time.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "list.pop(0) takes $O(N)$ time because removing the first element requires shifting all remaining elements to the left. collections.deque is ...",
        "is_correct": false
      }
    ],
    "explanation": "Dictionary keys must be hashable (their hash value must never change over their lifetime). Lists are mutable (elements can be added/removed, changing their identity), whereas tuples are immutable.",
    "lesson_ref": "10_SELF_ASSESSMENT_AND_CHALLENGES.md"
  }
]