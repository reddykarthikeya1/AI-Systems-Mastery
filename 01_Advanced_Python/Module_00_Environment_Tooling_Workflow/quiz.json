[
  {
    "id": 1,
    "tier": "Recall",
    "question": "Runtime Introspection: What is the programmatic difference between sys.prefix and sys.baseprefix when running inside a virtual environment versus system Python?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "In system/global Python, sys.prefix == sys.baseprefix. When a virtual environment is activated, sys.prefix points to the isolated environment directory (where the virtualenv's site-packages lives), while sys.baseprefix still points to the o",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "sys.prefix always points to the system Python installation directory, while sys.baseprefix points to wherever the currently active virtual environment lives -- the two names are the reverse of their actual meaning.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "sys.prefix and sys.baseprefix are always identical in every environment; the only reliable way to detect whether a virtual environment is active is checking os.environ['VIRTUAL_ENV'].",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "sys.baseprefix only exists inside a virtual environment; outside of one, accessing sys.baseprefix raises an AttributeError because there is no base interpreter to reference.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: In system/global Python, sys.prefix == sys.baseprefix. When a virtual environment is activated, sys.prefix points to the isolated environment directory (where the virtualenv's site-packages lives), while sys.baseprefix still points to the original parent Python installation.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 2,
    "tier": "Recall",
    "question": "Project Architecture: Why is the src-layout (src/myapp/) safer than the flat-layout (myapp/ in root) when running test suites with pytest?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "In a flat layout, running pytest automatically adds the current working directory (.) to sys.path[0]. This allows tests to import the uninstalled local folder directly, masking errors where the package was not properly packaged, built, or i",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The src-layout is safer only because it keeps test files physically separate from source files on disk; pytest's sys.path behavior is identical regardless of which layout is used.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "In a flat layout, pytest refuses to discover any tests at all unless a conftest.py exists at the repository root; the src-layout works around this by auto-generating one.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "src-layout prevents accidental imports of the wrong package version because pip always installs src-layout packages in editable mode by default, while flat-layout packages are always installed as regular non-editable copies.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: In a flat layout, running pytest automatically adds the current working directory (.) to sys.path[0]. This allows tests to import the uninstalled local folder directly, masking errors where the package was not properly packaged, built, or installed. The src-layout ensures tests only succeed if the package is installed into the environment.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 3,
    "tier": "Recall",
    "question": "Packaging Standards: What is the role of pyproject.toml under PEP 621, and what older configuration files does it replace?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "pyproject.toml provides a single, standardized configuration format for project metadata, dependencies, build backends, and tool settings. It replaces setup.py, setup.cfg, requirements.txt, MANIFEST.in, .flake8, pytest.ini, and black.toml.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "pyproject.toml only configures the build backend (e.g. setuptools or hatchling); dependencies, tool settings, and project metadata must still live in setup.cfg and requirements.txt alongside it.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "PEP 621 makes pyproject.toml a replacement specifically for requirements.txt; setup.py, setup.cfg, and tool-specific config files like pytest.ini remain necessary and are not affected.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "pyproject.toml is only read by pip during installation; tools like pytest, ruff, and mypy ignore it entirely and require their own dedicated configuration files to run.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: pyproject.toml provides a single, standardized configuration format for project metadata, dependencies, build backends, and tool settings. It replaces setup.py, setup.cfg, requirements.txt, MANIFEST.in, .flake8, pytest.ini, and black.toml.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 4,
    "tier": "Recall",
    "question": "Tooling Performance: What architectural reason makes uv and ruff 10x\u2013100x faster than traditional Python tools like pip, flake8, and black?",
    "category": "Recall: Architecture Concept",
    "options": [
      {
        "id": "opt_corr",
        "text": "uv and ruff are written in Rust and compiled to native machine code. They utilize multi-threaded file parsing, direct AST memory representations, global package caching with copy-on-write/reflinks, and zero-copy data structures, avoiding Py",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "uv and ruff are faster only because they cache results more aggressively than pip and flake8; both tool families are written in Python, but uv and ruff skip re-parsing unchanged files entirely.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "uv and ruff achieve their speed by running checks and installs in the cloud on Astral's servers and streaming back only the results, avoiding local CPU work entirely.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "The speed difference comes from uv and ruff dropping support for older Python versions; by targeting only Python 3.12+, they can skip backward-compatibility code paths that slow down pip and flake8.",
        "is_correct": false
      }
    ],
    "explanation": "Specification answer: uv and ruff are written in Rust and compiled to native machine code. They utilize multi-threaded file parsing, direct AST memory representations, global package caching with copy-on-write/reflinks, and zero-copy data structures, avoiding Python interpreter overhead.",
    "lesson_ref": "SELF_ASSESSMENT_AND_CHALLENGES.md"
  },
  {
    "id": 5,
    "tier": "Apply",
    "question": "Given the implementation in `test_core.py`, what is the exact evaluated result of:\n\n```python\npending_tasks[0].id\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "t1.id",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "tasks[0].id",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "t0.id",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "pending_tasks[1].id",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `t1.id` as verified in test suite `test_core.py`.",
    "lesson_ref": "test_core.py"
  },
  {
    "id": 6,
    "tier": "Apply",
    "question": "Given the implementation in `test_core.py`, what is the exact evaluated result of:\n\n```python\ntask.id\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "1",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "0",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "-1",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "None",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `1` as verified in test suite `test_core.py`.",
    "lesson_ref": "test_core.py"
  },
  {
    "id": 7,
    "tier": "Apply",
    "question": "Given the implementation in `test_core.py`, what is the exact evaluated result of:\n\n```python\nretrieved.title\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "\"Task 1\"",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "\"TASK 1\"",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "\"\"",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "None",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `\"Task 1\"` as verified in test suite `test_core.py`.",
    "lesson_ref": "test_core.py"
  },
  {
    "id": 8,
    "tier": "Apply",
    "question": "Given the implementation in `test_core.py`, what is the exact evaluated result of:\n\n```python\nlen(task_manager)\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "1",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "0",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "2",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "3",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `1` as verified in test suite `test_core.py`.",
    "lesson_ref": "test_core.py"
  },
  {
    "id": 9,
    "tier": "Diagnose",
    "question": "During triage in Environment Tooling Workflow, an engineer observes:\n\"Unvalidated CLI Input: Running python debuglab/brokencli.py high crashes with an unhandled traceback:\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "Raw int() call without try/except and without domain validation allows non-integer strings to crash the CLI with raw tracebacks and allows invalid negative priority levels.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "argparse automatically raises a ValueError instead of exiting with SystemExit when given a non-integer argument, so the crash actually originates from an uncaught ValueError deep inside the argparse library itself.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The CLI defines the priority argument with type=str, so int() conversion never runs at all; the traceback is instead raised later when the string priority is used in a numeric comparison.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "click's Choice type was used to restrict priority to a fixed list of allowed strings; passing 'high' should have been rejected by click's own validation before ever reaching the business logic.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: Raw int() call without try/except and without domain validation allows non-integer strings to crash the CLI with raw tracebacks and allows invalid negative priority levels.\n\nFix Resolution:\nUse structured argument parsing (argparse or click) with explicit type validation:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 10,
    "tier": "Diagnose",
    "question": "During triage in Environment Tooling Workflow, an engineer observes:\n\"ImportError / Path Flakiness: sys.path.append(\"./src\") fails to resolve modernapp when invoked from outside the root directory or when packaged.\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "open(\"config.toml\") looks in Path.cwd(), not the directory where the code lives.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The package is missing an __init__.py file in src/modernapp, so Python does not recognize it as an importable package regardless of the current working directory.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "pip install -e . registers the package under a build-time snapshot of sys.path; once distributed as a wheel, editable installs silently stop resolving until reinstalled.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "sys.path.append() adds paths to the end of the search order, so a same-named third-party package on PyPI shadows the local modernapp module and is imported instead.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: open(\"config.toml\") looks in Path.cwd(), not the directory where the code lives.\n\nFix Resolution:\nAnchor configuration resolution to the package or module root:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 11,
    "tier": "Diagnose",
    "question": "During triage in Environment Tooling Workflow, an engineer observes:\n\"FileNotFoundError: Running the script from anywhere other than the exact folder containing config.toml crashes immediately:\".\nWhat is the verified root cause?",
    "category": "Diagnose: Bug Triage",
    "options": [
      {
        "id": "opt_corr",
        "text": "Using sys.path.append(\"./src\") relies on whatever current working directory (os.getcwd()) happens to be when the command runs. If executed from a parent or sibling folder, ./src points to a non-existent path.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "TOML files must be located inside the project's site-packages directory to be discoverable; config.toml works only when Python is invoked as `python -m modernapp` instead of as a direct script path.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The script opens the file in text mode without specifying encoding='utf-8', and on Windows this raises FileNotFoundError instead of a UnicodeDecodeError when non-ASCII bytes are present.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "config.toml was accidentally excluded from the tracked files and .gitignored, so it only exists locally in the developer's original working directory and was never actually shipped with the project.",
        "is_correct": false
      }
    ],
    "explanation": "Root Cause: Using sys.path.append(\"./src\") relies on whatever current working directory (os.getcwd()) happens to be when the command runs. If executed from a parent or sibling folder, ./src points to a non-existent path.\n\nFix Resolution:\nInstall the project in editable mode (pip install -e . or uv pip install -e .) so that standard package imports work everywhere without manual sys.path tampering. For standalone script pathing, anchor to file:",
    "lesson_ref": "debug_lab/ANSWERS.md"
  }
]