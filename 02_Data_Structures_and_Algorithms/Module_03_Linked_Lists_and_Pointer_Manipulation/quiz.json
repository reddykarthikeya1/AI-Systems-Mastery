[
  {
    "id": 1,
    "question": "In 03 Linked Lists and Pointer Manipulation triage of defect 'Reversal returns a one-element list', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "cur.next = prev overwrites the only pointer to the remainder of the list. The very next statement then reads that same field, so cur walks backwards into prev instead of forwards, the loop ends after one step, and everything past the head is unreacha",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "cur.next = prev overwrites the only pointer to the remainder of the list. The very next statement then reads that same field, so cur walks backwards into prev instead of forwards, the loop ends after one step, and everything past the head is unreacha",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 2,
    "question": "In 03 Linked Lists and Pointer Manipulation triage of defect 'Cycle detection never detects a cycle', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "Floyd's algorithm works because the two pointers move at different speeds: the gap between them shrinks by one node per iteration, so inside a cycle they must eventually coincide. Moving both by one keeps the gap constant forever, and they never meet",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "Floyd's algorithm works because the two pointers move at different speeds: the gap between them shrinks by one node per iteration, so inside a cycle they must eventually coincide. Moving both by one keeps the gap constant forever, and they never meet",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 3,
    "question": "In 03 Linked Lists and Pointer Manipulation triage of defect 'The middle of an even-length list is off by one', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "The extra fast.next.next is not None term makes the loop exit one iteration sooner whenever the length is even, so slow lands on the first middle node rather than the second. Odd-length lists are unaffected, which is exactly why this survives casual ",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "The extra fast.next.next is not None term makes the loop exit one iteration sooner whenever the length is even, so slow lands on the first middle node rather than the second. Odd-length lists are unaffected, which is exactly why this survives casual ",
    "lesson_ref": "debug_lab/ANSWERS.md"
  }
]