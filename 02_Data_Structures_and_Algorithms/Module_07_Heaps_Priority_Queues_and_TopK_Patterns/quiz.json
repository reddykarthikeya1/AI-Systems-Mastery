[
  {
    "id": 1,
    "question": "In 07 Heaps Priority Queues and TopK Patterns triage of defect 'K-th largest returns the k-th smallest', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "Negating the values turns heapq into a max-heap, so heappop removes the largest element \u2014 and the survivors are the k smallest. The function computes the k-th smallest and calls it the k-th largest. For k=1 and for all-equal inputs the two answers co",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "Negating the values turns heapq into a max-heap, so heappop removes the largest element \u2014 and the survivors are the k smallest. The function computes the k-th smallest and calls it the k-th largest. For k=1 and for all-equal inputs the two answers co",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 2,
    "question": "In 07 Heaps Priority Queues and TopK Patterns triage of defect 'The merge stops as soon as any one list is exhausted', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "Exhausting one input list is a completely ordinary event \u2014 it happens k times in any successful merge. Treating it as a termination condition ends the merge at the first one, discarding everything still queued in the other lists. The output is correc",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "Exhausting one input list is a completely ordinary event \u2014 it happens k times in any successful merge. Treating it as a termination condition ends the merge at the first one, discarding everything still queued in the other lists. The output is correc",
    "lesson_ref": "debug_lab/ANSWERS.md"
  },
  {
    "id": 3,
    "question": "In 07 Heaps Priority Queues and TopK Patterns triage of defect 'The median is wrong for every even-length prefix', what is the underlying failure mechanism under load?",
    "category": "Defect Triage",
    "options": [
      {
        "id": "opt_correct",
        "text": "After rebalancing, len(lower) == len(upper) means an even number of values, and the median is the average of the two roots. >= sends that case down the odd-count branch, which returns the lower of the two middle values instead of their mean. Every od",
        "is_correct": true
      },
      {
        "id": "opt_dist_1",
        "text": "The routine raises an uncaught syntax exception immediately upon interpreter startup.",
        "is_correct": false
      },
      {
        "id": "opt_dist_2",
        "text": "The thread pool deadlocks because mutex acquisition order is non-deterministic.",
        "is_correct": false
      },
      {
        "id": "opt_dist_3",
        "text": "The operating system terminates the process due to kernel memory bounds.",
        "is_correct": false
      }
    ],
    "explanation": "After rebalancing, len(lower) == len(upper) means an even number of values, and the median is the average of the two roots. >= sends that case down the odd-count branch, which returns the lower of the two middle values instead of their mean. Every od",
    "lesson_ref": "debug_lab/ANSWERS.md"
  }
]