[
  {
    "id": 1,
    "tier": "Apply",
    "question": "Given the implementation in `10_Equivalence_Relations_and_Partitions.md`, what is the exact evaluated result of:\n\n```python\n{x for x in A if same_mod_3(x, 0)}\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "{x for x in A if same_mod_3(x, 3)}",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "{x for x in A if same_mod_3(x, 0)}",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "{x for x in A if same_mod_3(x, 1)}",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "{x for x in A if not same_mod_3(x, 3)}",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `{x for x in A if same_mod_3(x, 3)}` as verified in test suite `10_Equivalence_Relations_and_Partitions.md`.",
    "lesson_ref": "10_Equivalence_Relations_and_Partitions.md"
  },
  {
    "id": 2,
    "tier": "Apply",
    "question": "Given the implementation in `11_Functions_as_Special_Relations.md`, what is the exact evaluated result of:\n\n```python\nlen(f)\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "len(A)",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "len(B)",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "len(set(f.values()))",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "len(A) - 1",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `len(A)` as verified in test suite `11_Functions_as_Special_Relations.md`.",
    "lesson_ref": "11_Functions_as_Special_Relations.md"
  },
  {
    "id": 3,
    "tier": "Apply",
    "question": "Given the implementation in `05_Complements_and_the_Universal_Set.md`, what is the exact evaluated result of:\n\n```python\nU2 - W\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "{\"a\", \"c\", \"e\", \"f\"}",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "{\"a\", \"c\", \"d\", \"e\", \"f\"}",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "{\"b\", \"d\"}",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "{\"a\", \"c\", \"e\"}",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `{\"a\", \"c\", \"e\", \"f\"}` as verified in test suite `05_Complements_and_the_Universal_Set.md`.",
    "lesson_ref": "05_Complements_and_the_Universal_Set.md"
  },
  {
    "id": 4,
    "tier": "Apply",
    "question": "Given the implementation in `01_Why_Sets_Are_the_Vocabulary_of_ML.md`, what is the exact evaluated result of:\n\n```python\nset(rows)\n```",
    "category": "Apply: Code Execution",
    "options": [
      {
        "id": "opt_corr",
        "text": "{1, 3, 7, 9}",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "[1, 3, 7, 9]",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "{1, 1, 3, 7, 9}",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "{1, 3, 9}",
        "is_correct": false
      }
    ],
    "explanation": "Evaluates to `{1, 3, 7, 9}` as verified in test suite `01_Why_Sets_Are_the_Vocabulary_of_ML.md`.",
    "lesson_ref": "01_Why_Sets_Are_the_Vocabulary_of_ML.md"
  },
  {
    "id": 5,
    "tier": "Diagnose",
    "question": "In Set Language for Machine Learning, what failure mode triggers the following observed symptom?\n\"Failure scenario: 'np.intersect1d reordered my rows'\"",
    "category": "Diagnose: Failure Analysis",
    "options": [
      {
        "id": "opt_corr",
        "text": "All of NumPy's set routines return sorted, deduplicated arrays. Using the result to index back into the original data reorders rows relative to every parallel array that was not reordered with it.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The accumulator starts as `{}`, which Python parses as an empty dictionary rather than an empty set. It is falsy and has length 0 just like a real empty set, so the bug passes every check until a set-only method like `.add` is called.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The leakage check tests that the row indices in train and test are disjoint. A positional split always produces disjoint row indices, so the assertion passes even though the same patient's other rows appear in both splits.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "Rows with a missing value in the grouping column are dropped by `groupby` before the per-group sums are taken. The individual group totals are each correct, but together they no longer account for every row in the full total.",
        "is_correct": false
      }
    ],
    "explanation": "Diagnostic Analysis: All of NumPy's set routines return sorted, deduplicated arrays. Using the result to index back into the original data reorders rows relative to every parallel array that was not reordered with it.\nSelect with a boolean mask instead: mask = np.isin(ids, wanted), then apply that same mask to every parallel array. Order and alignment are preserved.",
    "lesson_ref": "TROUBLESHOOTING_AND_EDGE_CASES.md"
  },
  {
    "id": 6,
    "tier": "Diagnose",
    "question": "In Set Language for Machine Learning, what failure mode triggers the following observed symptom?\n\"Failure scenario: '{} is not the empty set'\"",
    "category": "Diagnose: Failure Analysis",
    "options": [
      {
        "id": "opt_corr",
        "text": "In Python {} is an empty dictionary. The empty set is set(). Both are falsy and both have length 0, so the mistake survives every truthiness and length check and only surfaces when a set method is called.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The leakage check tests that the row indices in train and test are disjoint. A positional split always produces disjoint row indices, so the assertion passes even though the same patient's other rows appear in both splits.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Rows with a missing value in the grouping column are dropped by `groupby` before the per-group sums are taken. The individual group totals are each correct, but together they no longer account for every row in the full total.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "`np.intersect1d` always returns its result sorted, so using that result to index back into the original arrays silently reorders the rows relative to any parallel array that was not indexed the same way.",
        "is_correct": false
      }
    ],
    "explanation": "Diagnostic Analysis: In Python {} is an empty dictionary. The empty set is set(). Both are falsy and both have length 0, so the mistake survives every truthiness and length check and only surfaces when a set method is called.\nUse set(). If you want an empty set literal for readability, there is not one - this is a genuine wart in the syntax.",
    "lesson_ref": "TROUBLESHOOTING_AND_EDGE_CASES.md"
  },
  {
    "id": 7,
    "tier": "Diagnose",
    "question": "In Set Language for Machine Learning, what failure mode triggers the following observed symptom?\n\"Failure scenario: 'Per-group totals do not reconstruct the overall total'\"",
    "category": "Diagnose: Failure Analysis",
    "options": [
      {
        "id": "opt_corr",
        "text": "The grouping key is not inducing a partition. Rows whose key is null or missing fall into no group, so the blocks do not cover the data.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The accumulator starts as `{}`, which Python parses as an empty dictionary rather than an empty set. It is falsy and has length 0 just like a real empty set, so the bug passes every check until a set-only method like `.add` is called.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "The leakage check tests that the row indices in train and test are disjoint. A positional split always produces disjoint row indices, so the assertion passes even though the same patient's other rows appear in both splits.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "`np.intersect1d` always returns its result sorted, so using that result to index back into the original arrays silently reorders the rows relative to any parallel array that was not indexed the same way.",
        "is_correct": false
      }
    ],
    "explanation": "Diagnostic Analysis: The grouping key is not inducing a partition. Rows whose key is null or missing fall into no group, so the blocks do not cover the data.\nAssert sum(per group) == sum(all) before trusting any grouped aggregate, and decide explicitly what a missing key means rather than letting the engine choose.",
    "lesson_ref": "TROUBLESHOOTING_AND_EDGE_CASES.md"
  },
  {
    "id": 8,
    "tier": "Diagnose",
    "question": "In Set Language for Machine Learning, what failure mode triggers the following observed symptom?\n\"Failure scenario: 'A split passes every check and the model still leaks'\"",
    "category": "Diagnose: Failure Analysis",
    "options": [
      {
        "id": "opt_corr",
        "text": "The assertion is over row indices, which are disjoint by construction for any positional split. The unit of independence - patient, user, document - can still appear on both sides.",
        "is_correct": true
      },
      {
        "id": "opt_1",
        "text": "The accumulator starts as `{}`, which Python parses as an empty dictionary rather than an empty set. It is falsy and has length 0 just like a real empty set, so the bug passes every check until a set-only method like `.add` is called.",
        "is_correct": false
      },
      {
        "id": "opt_2",
        "text": "Rows with a missing value in the grouping column are dropped by `groupby` before the per-group sums are taken. The individual group totals are each correct, but together they no longer account for every row in the full total.",
        "is_correct": false
      },
      {
        "id": "opt_3",
        "text": "`np.intersect1d` always returns its result sorted, so using that result to index back into the original arrays silently reorders the rows relative to any parallel array that was not indexed the same way.",
        "is_correct": false
      }
    ],
    "explanation": "Diagnostic Analysis: The assertion is over row indices, which are disjoint by construction for any positional split. The unit of independence - patient, user, document - can still appear on both sides.\nAssert over the group ids: assert units(train) & units(test) == set(). Defining units correctly is a question about the data, not the code.",
    "lesson_ref": "TROUBLESHOOTING_AND_EDGE_CASES.md"
  }
]